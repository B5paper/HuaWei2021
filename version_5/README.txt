

bug 调试：
1. 服务器的 id 和输出顺序一直对不上号，最后发现不能跨天处理序列，或者购买服务器。因为假如有一个 add 序列有一天半的长度，处理完这个 add 序列后，我们已经给这天新买的服务器标了 id，在处理第 2 天的半天 add 序列时，假如买了某个相同类型的服务器，那么又会给相同类型的服务器标个新 id。但是在最后输出时，相同类型的服务器是连在一起的，这意味着它们的 id 也是在一起的。因此整个 id 顺序就都混乱了。

经验：
1. 接口真是个好东西。把接口定义好，就算接口两端改得面目全非，对用到接口的地方也影响不大。