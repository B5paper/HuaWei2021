#include "eval.h"

ulong get_cost(ServerStat &server_stat, ServerSpecList &server_specs)
{
    ulong cost;
    for (int i = 0; i < server_stat.types.size(); ++i)
    {
        cost += server_specs.
    }
} 
