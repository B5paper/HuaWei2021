1. 用 scanf 和 printf 重写了 I/O，据说这个速度比 iostream 要快
2. 完成了第一个可以成功提交的版本。第 142 天的第 671，672 个请求处，有个很隐蔽的 bug。在调试模式下可以正常运行，在非调试模式下会陷入死循环。修复了就能正常提交了
3. 优化了存储 vm_stat 的数据结构，采用 unordered_map 来存储
4. 删除了许多无用的代码

问题：
1. 没有算法的优化。前期需要考虑到服务器的选型问题，因为 add 操作比较多；后期需要考虑到迁移虚拟机和关闭服务器的问题，因为 del 操作比较多。